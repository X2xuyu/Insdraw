# 🎨 Insdraw

**InstaAutoDraw** is a tool that automatically draws your PNG images inside **Instagram Story Draw Mode** using **ADB (Android Debug Bridge)**.  
Simply connect your Android phone, choose a PNG, and the app will trace it line by line — perfectly and automatically ⚡

---

## 🖼️ Preview
![image alt](https://github.com/X2xuyu/Insdraw/blob/main/forgithub.png?raw=true)

Example result:  
- Adjustable **Blur**, **Sample Step**, and **Segment Duration** for precision and speed.  
- Real-time **Progress Bar** and **Logging Console**.  
- Supports any PNG size (recommended: 720×1612 for most phone screens).

---

## ⚙️ Features
✅ Convert PNG images to Instagram draw strokes automatically  
✅ Connect and control your Android device via **ADB**  
✅ Tune drawing precision (blur, step size, speed)  
✅ Auto device detection  
✅ Modern PyQt5 GUI (with theme support)  
✅ Build as `.exe` with custom app icon  

---

## 🧩 Installation

### 1. Install Python dependencies
```bash
pip install -r requirements.txt
